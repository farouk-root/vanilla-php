function generateQRCode() {
    var eventData = document.getElementById('eventInput').value;

    if (eventData.trim() !== '') {
        var qrcode = new QRCode(document.getElementById("qrcode"), {
            text: eventData,
            width: 200,
            height: 200
        });
    }
}
